# 📘 Автоматизированное тестирование eNotes

Этот проект включает два вида автотестов:

- ✅ **API тесты** для проверки REST эндпоинтов
- ✅ **E2E тесты** для UI тестирования с использованием Playwright

---

## 📦 Установка

1. Клонируйте репозиторий:

   ```bash
   git clone <your-repo-url>
   cd api-enotes-tests
   ```

2. Установите зависимости:

   ```bash
   npm install
   ```

3. Установите Allure CLI (если ещё не установлен):

   ```bash
   npm install -g allure-commandline --save-dev
   ```

4. Создайте файл `.env` в корне проекта:

   ```env
   BASE_URL=https://enotes.pointschool.ru
   ENOTES_USERNAME=ваш_логин
   ENOTES_PASSWORD=ваш_пароль
   ```

---

## 🚀 Запуск API тестов

```bash
npm run test:api
```

Тесты написаны с использованием **Mocha**, **Chai**, **Axios**, валидации по **JSON Schema** через **Ajv**, с интеграцией **Allure Mocha Reporter** для подробной отчётности. Инициализация Allure происходит через `setup.ts`.

### 📊 Allure отчёт по API тестам

1. Сгенерировать отчёт:

   ```bash
   npm run allure:generate
   ```

2. Открыть отчёт в браузере:

   ```bash
   npm run allure:open
   ```

---

## 🧰 Запуск E2E тестов (UI)

Тесты для пользовательского интерфейса написаны с использованием **Playwright** и архитектуры **Page Object Model**.

Запуск всех E2E тестов:

```bash
npm run test:e2e
```

### Запуск по браузерам

```bash
npm run test:chrome     # Chromium
npm run test:firefox    # Firefox
npm run test:safari     # WebKit (Safari)
npm run test:all        # Все браузеры
```

---

## 📁 Структура проекта

```
api-enotes-tests/
├── api/
│   ├── tests/                # API тесты
│   │   └— setup.ts           # Настройка Allure для Mocha
│   ├── utils/                # Утилиты для авторизации и запросов
│   ├── data/                 # Ожидаемые значения
│   └── schemas/              # JSON Schema для валидации
│
├── e2e/
│   ├── tests/                # E2E тесты на Playwright
│   ├── pages/                # Page Object файлы
│   ├── locators/             # Локаторы для разных страниц
│   └── data/                 # Тестовые данные
│
├── .env
├── package.json
├── tsconfig.json
└── README.md
```

---

## 📒 Скрипты

| Команда                   | Описание                           |
| ------------------------- | ---------------------------------- |
| `npm run test:api`        | Запуск API тестов с Allure отчётом |
| `npm run test:e2e`        | Запуск E2E тестов                  |
| `npm run test:chrome`     | E2E тесты в Chromium               |
| `npm run test:firefox`    | E2E тесты в Firefox                |
| `npm run test:safari`     | E2E тесты в Safari                 |
| `npm run test:all`        | E2E тесты во всех браузерах        |
| `npm run allure:generate` | Генерация отчёта Allure            |
| `npm run allure:open`     | Открытие Allure отчёта в браузере  |

---

## 🔐 Авторизация

Все тесты используют данные из `.env` файла для авторизации:

- `ENOTES_USERNAME`
- `ENOTES_PASSWORD`
- `BASE_URL`

---

Если возникнут вопросы по запуску или структуре — пиши в telegram(@valerik1986)
